#include <stdio.h>

int main(void) {
	int n;
	char c;
	c = getchar();
	//scanf("%d", &n);
	// rewind(stdin);
	// fflush(stdin);
	c = getchar();       // 얘는 엔터값을 받아옴
	//scanf(" %c", &c);

	return 0;

}
