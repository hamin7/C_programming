#include <stdio.h>

int main(void) {
	int n;
	char c;
	int year, month, day;
	scanf("%d - %d - %d", &year, &month, &day);
	scanf("%d", &n);
	// rewind(stdin);
	// fflush(stdin);
	scanf(" %c", &c);

	return 0;

}
